/* MessageFunnel — frontend configuration.
   Set this to your hosted API base URL (https, no trailing slash) and the
   Pages site goes LIVE. While empty, the site runs in DEMO mode using
   demo-data.json.

   Notes:
   - GitHub Pages is served over HTTPS, so the API must also be https
     (browsers block mixed content). http://localhost only works when you
     open this page locally or pass ?api= override on a non-https origin.
   - You can also test without editing: append ?api=http://localhost:8800 */
window.MESSAGE_FUNNEL_API = "";
